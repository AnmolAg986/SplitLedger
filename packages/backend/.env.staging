# Database (Managed via GitHub Secrets / Vault in CI/CD)
# DATABASE_URL=postgres://user:pass@staging-db:5432/splitledger_staging

# Redis
# REDIS_URL=redis://staging-redis:6379

# App
PORT=3000
NODE_ENV=staging
CORS_ORIGIN=https://staging.splitledger.com
LOG_LEVEL=info
